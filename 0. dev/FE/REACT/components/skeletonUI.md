## SkeletonUI

```css
@keyframes skeleton {
  from {
    filter: brightness(1);
  }

  50% {
    filter: brightness(200%);
  }
  to {
    filter: brightness(1);
  }
}
filter: brightness(1);
animation: skeleton 1s infinite;
```
